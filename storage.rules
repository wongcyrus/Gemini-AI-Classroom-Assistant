rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /screenshots/{classId}/{studentId}/screenshot.jpg {
      allow read: if request.auth.token.role == 'teacher';
      allow write: if request.auth.uid == studentId;
    }
  }
}
